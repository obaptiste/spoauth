import {atom} from "recoil";

export const selectedPlaylistState = atom({
    key: 'selectedPlaylistState',
    default: 'null',
});

export const playlistState = atom({
    key: 'playlistState',
    default: 'null',
});

export const playlistIdState = atom({
    key: "playlistIdState",
    default: '77BeZTcVWYFV4irB8QMvFP'
});